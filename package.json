{
  "name": "cycloeval2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build --prefix server && npm run dev --prefix client & npm run start --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "clean": "rm -rf client/dist server/dist"
  }
}
